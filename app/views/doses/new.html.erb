<div class = container>
<h2>🍸 Add Ingredient for <%=@cocktail.name%>🍸</h2>

	<%= simple_form_for [@cocktail, @dose] do |f| %>
	<%= f.input :description %>
	<%= f.association :ingredient %>
	<%= f.submit %>
	<% end %>
</div>
